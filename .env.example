# LM Studio API endpoint (default port for LM Studio's built-in server)
LM_STUDIO_URL=http://localhost:1234

# Default model to use when no model is selected in the UI
# This should match a model loaded in LM Studio
DEFAULT_MODEL=google/translategemma-27b-it

# Server port (used by TanStack Start dev server)
PORT=3000
